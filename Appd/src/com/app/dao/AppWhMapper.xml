<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.app.dao.AppWhMapper">
<select id="selApp" parameterType="int"  resultType="com.app.pojo.AppInfo">
select * from app_info where id=#{id}
</select>
<insert id="addApp" parameterType="com.app.pojo.AppInfo"> 
insert into app_info(id,softwareName,APKName,supportROM,interfaceLanguage,softwareSize,updateDate,devId,appInfo,status,onSaleDate,offSaleDate,flatformId,categoryLevel3,downloads,createdBy,creationDate,modifyBy,modifyDate,
categoryLevel1,categoryLevel2,logoPicPath,logoLocPath,versionId) 
values(#{id},#{softwareName},#{APKName},#{supportROM},#{interfaceLanguage},#{softwareSize},#{updateDate},#{devId},#{appInfo},#{status},#{onSaleDate},#{offSaleDate},#{flatformId},#{categoryLevel3},#{downloads},#{createdBy},#{creationDate},#{modifyBy},#{modifyDate},#{categoryLevel1},
#{categoryLevel2},#{logoPicPath},#{logoLocPath},#{versionId})
</insert>
<select id="selAllApp" resultType="com.app.pojo.AppInfo">
select * from app_info
</select>
<select id="pagebyId"  resultType="com.app.pojo.AppInfo">
select * from app_info limit (currentPageNo-1)*pageSize,pageSize
</select>
<select id="selVersion" resultType="com.app.pojo.AppVersion">
select * from app_version
</select>
<select id="flatForm" parameterType="String" resultType="com.app.pojo.DataDictionary">
select * from data_dictionary where typeName=#{typeName}
</select>
<select id="status" parameterType="String" resultType="com.app.pojo.DataDictionary">
select * from data_dictionary where typeName=#{typeName}
</select>
<select id="selAllCat"  resultType="com.app.pojo.AppCategory">
select * from app_category
</select>

</mapper>